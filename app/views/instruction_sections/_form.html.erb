<%= form_with(model: instruction_section) do |form| %>
  <% if instruction_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(instruction_section.errors.count, "error") %> prohibited this instruction_section from being saved:</h2>

      <ul>
        <% instruction_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
